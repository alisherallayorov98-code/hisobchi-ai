# FlowPilot Landing Page MVP

Simple static starter website for a cross-industry operations platform concept.

## Run locally

Open `index.html` directly in browser or serve with any static server.

Example:

```bash
python3 -m http.server 8080
```

Then open http://localhost:8080.
